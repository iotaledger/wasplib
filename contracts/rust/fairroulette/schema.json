{
  "name": "FairRoulette",
  "types": {
    "Bet": {
      "amount": "Int64",
      "better": "AgentId",
      "number": "Int64"
    }
  },
  "vars": {
    "bets": "[]Bet",
    "lastWinningNumber": "Int64",
    "lockedBets": "[]Bet",
    "playPeriod": "Int64"
  },
  "funcs": {
    "placeBet": {
      "number": "Int64 // the number a better bets on"
    },
    "lockBets": {
      "#grant": "self // only SC itself can invoke this function"
    },
    "payWinners": {
      "#grant": "self // only SC itself can invoke this function"
    },
    "playPeriod": {
      "#grant": "creator // only SC creator can update the play period",
      "playPeriod": "Int64 // number of minutes in one playing round"
    }
  },
  "views": {
    "lastWinningNumber": {
    }
  }
}
