{
  "name": "fairroulette",
  "types": {
    "BetInfo": {
      "amount": "Int",
      "better": "Agent",
      "number": "Int"
    }
  },
  "vars": {
    "bets": "[]BetInfo]",
    "lastWinningNumber": "Int",
    "lockedBets": "[]BetInfo]",
    "playPeriod": "Int"
  },
  "funcs": {
    "placeBet": {
      "number": "Int // the number a better bets on"
    },
    "lockBets": {
      "#owner": "contract // only SC itself can invoke this function"
    },
    "payWinners": {
      "#owner": "contract // only SC itself can invoke this function"
    },
    "playPeriod": {
      "#owner": "creator // only SC creator can update the play period",
      "playPeriod": "Int // number of minutes in one playing round"
    }
  },
  "views": {
  }
}
